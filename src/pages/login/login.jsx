import * as React from "react";
import Form from "../../Componentes/FormLogin";

import "./login.scss";

const Login = (props) => {
  return (
    <div className="login">
      <Form />
    </div>
  );
};
export default Login;
